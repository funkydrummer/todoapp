$('#my-tab-content').html("<%= escape_javascript render(:partial => 'items/content', :locals => { :categories => @categories}) %>");

$('.items-list').sortable({
  axis: 'y',
  update: function() {
    return $.post($(this).data('update-url'), $(this).sortable('serialize'));
  }
});
